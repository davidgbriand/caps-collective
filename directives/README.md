# Directives

This directory contains Standard Operating Procedures (SOPs) written in Markdown.
These directives define the goals, inputs, tools to use, outputs, and edge cases for the agent.

## Structure
- Each directive should be a separate Markdown file.
- Use natural language to describe instructions.
