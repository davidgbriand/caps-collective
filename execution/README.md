# Execution

This directory contains deterministic Python scripts used by the agent system.

## Structure
- Scripts should be self-contained and perform specific actions.
- Use `.env` for configuration and secrets.
